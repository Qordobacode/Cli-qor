package file

import (
	"testing"
)

func Test_Pattern(t *testing.T) {
	//text := "<language_code><filename>.<extension>"
	//regValues, _ := regexp.Compile("/<.*?>/")
	//allString := regValues.FindAllString(regValues)
}
